<p id="notice"><%= notice %></p>

<h1>Listing Productivities</h1>

<%= will_paginate %>
<ul class="clients">
  <% @productivities.each do |productivity| %>
    <li>
      <%= render partial: "productivity_tag", object: productivity, as: "productivity" %>
    </li>
  <% end %>
</ul>
<%= will_paginate %>

<br>

<%= link_to 'New Productivity', new_productivity_path %>
